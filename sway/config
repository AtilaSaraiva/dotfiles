# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

#================================================
#==================Variables=====================
#================================================

include /etc/sway/config.d/*

set $mod Mod1
set $term kitty
set $browser QT_QPA_PLATFORM=wayland qutebrowser
set $teams flatpak run com.github.IsmaelMartinez.teams_for_linux
#set $music google-chrome-stable --app="https://music.youtube.com/"
set $music spotify
set $todo  google-chrome-stable --app="https://trello.com/b/PtPUWPeM/doutorado-e-vida-profissional"

set $firefox "MOZ_ENABLE_WAYLAND=1 firefox"

set $site1 "https://novopit.clear.com.br/Operacoes/RendaVariavel/Basico"
set $site2 "https://docs.google.com/document/d/1iwhbdTI96cPlG5GffoVM1aKnvhNzmWEJ1NxYFMQ0G0s/edit"
set $site3 "https://www.fundamentus.com.br/resultado.php?setor=5"
set $site4 "https://eduardocavalcanti.com/"
set $site5 "https://vicenteguimaraes.penserico.com/dashboard.pr"
set $site6 "https://docs.google.com/spreadsheets/d/15XVyZg2Ex_1nTYfA9W8qUAl1NRAxo9g9HCSlAeqwVhw/edit?folder=0AMLx_qjqAT5PUk9PVA#gid=0"

set $mon0 DP-2
set $mon1 HDMI-A-2


bindsym $mod+ccedilla exec ~/.bin/toggleFreesync

#================================================
#====================Fonts=======================
#================================================
#
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:RobotoMono 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap

# font, it doesn’t scale on retina/hidpi displays.


#================================================
#====================Apps========================
#================================================

#rofi favorite gui menu
#bindsym $mod+i exec ~/.Tools/bin/rofi-fav-gui-i3.sh

# run rofi frontend of buku bookmark manager
bindsym $mod+o exec buku_run

# add url on the clipboard to buku db
bindsym $mod+Shift+o exec ~/.bin/bookmarkadd

# Kill all process that might use cpu clocks in order to play games
bindsym $mod+Delete exec ~/.bin/kill4game

# Reopen then after finished gaming
bindsym $mod+Insert exec ~/.bin/aftergameopen

# start a terminal
bindsym $mod+Return exec $term

# start dropdown terminal
#bindsym $mod+t nop kitti3
bindsym $mod+t exec $term --class "dropdown_terminal" -e "zsh"

# open vifm
bindsym $mod+n exec kitty --class lf -e lf
bindsym $mod+Shift+n exec kitty --class=notes vifm ~/Files/Mega/phd/notes

# kill focused window
bindsym $mod+Shift+q kill

# open broser
bindsym $mod+b exec $browser

#start dmenu
#bindsym $mod+d exec dmenu_run -nb black -nf white -sb white -sf black -fn RobotoMono:size=10

#start rofi
 #bindsym $mod+d exec rofi -show drun -show-icons -hide-scrollbar -lines 10 -eh 1 -width 40 -location 8 -xoffset 170 -yoffset 70 -padding 30 -disable-history -font "RobotoMono 18"
#start ulauncher
#set $menu rofi -show drun -show-icons -icon-theme "Papirus-Dark"
set $menu sirula
#bindsym $mod+d exec ulauncher-toggle
bindsym $mod+d exec $menu


# rofi with change window feature (like select window in tmux)
# bindsym $mod+Shift+semicolon exec rofi -show window -location 5 -width 50 -lines 15 -disable-history -font "RobotoMono 12"
# bindsym $mod+Shift+semicolon exec rofi -show window -location 5 -width 50 -lines 15 -disable-history -font "RobotoMono 12"

#================================================
#================Window Manag====================
#================================================

workspace_auto_back_and_forth yes

# turn off second monitor
bindsym $mod+backslash output $mon1 toggle

# Toggle focus between monitors
bindsym $mod+u focus output left

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# Show the sup-mail scratchpad window, if any.
bindsym $mod+Ctrl+t [app_id="kotatogramdesktop"] scratchpad show
bindsym $mod+Ctrl+b [app_id="com.rafaelmardojai.Blanket"] scratchpad show
#bindsym $mod+Ctrl+d [class="Transmission-gtk"] scratchpad show
#bindsym $mod+Ctrl+a [app_id="org.keepassxc.KeePassXC"] scratchpad show
bindsym $mod+Ctrl+a [class="Bitwarden"] scratchpad show
#bindsym $mod+Ctrl+q [class="youtube-music-desktop-app"] scratchpad show
bindsym $mod+Ctrl+d [app_id="org.qbittorrent.qBittorrent"] scratchpad show
bindsym $mod+Ctrl+p [app_id="planner"] scratchpad show
bindsym $mod+Ctrl+s [instance="teams-for-linux"] scratchpad show
#bindsym $mod+Ctrl+q [instance="music.youtube.com"] scratchpad show
bindsym $mod+Ctrl+q [instance="spotify"] scratchpad show
bindsym $mod+Ctrl+w [instance="trello.com__b_PtPUWPeM_doutorado-e-vida-profissional"] scratchpad show
bindsym $mod+Ctrl+r [class="Slack"] scratchpad show
bindsym $mod+Ctrl+e [class="Element"] scratchpad show

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# change focus (try with vim keybiding for practice vim editor) (i3wm keybinding movement scheme conflict with vim wich will be disaster to use i3wm+vim together)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
#bindsym $mod+semicolon split h

# split in vertical orientation
#bindsym $mod+v split v

# split toggle
bindsym $mod+s split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle, sticky disable

# put floating window in the corner and make it sticky
#bindsym $mod+Ctrl+s floating enable, sticky enable, resize set 711 400, move position 1209px 680px
#bindsym $mod+Ctrl+s fullscreen disable, floating enable, resize set 711 400, resize grow 1px
#bindsym $mod+Ctrl+s floating enable, sticky enable, resize set 711 400, move position 1209px 680px, focus tiling

bindsym $mod+Ctrl+f focus floating, fullscreen enable
bindsym $mod+Ctrl+c fullscreen disable, focus tiling

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# make a floating windows sticky
bindsym $mod+Shift+s sticky toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $map-to-active swaymsg input type:tablet_tool map_to_output `swaymsg -t get_outputs | jq -r '.[] | select(.focused == true) | .name'`

# switch to workspace
bindsym $mod+1 workspace 1, focus tiling; exec $map-to-active
bindsym $mod+2 workspace 2, focus tiling; exec $map-to-active
bindsym $mod+3 workspace 3, focus tiling; exec $map-to-active
bindsym $mod+4 workspace 4, focus tiling; exec $map-to-active
bindsym $mod+5 workspace 5, focus tiling; exec $map-to-active
bindsym $mod+6 workspace 6, focus tiling; exec $map-to-active
bindsym $mod+7 workspace 7, focus tiling; exec $map-to-active
bindsym $mod+8 workspace 8, focus tiling; exec $map-to-active
bindsym $mod+9 workspace 9, focus tiling; exec $map-to-active
bindsym $mod+v workspace 8, workspace 9, focus tiling; exec $map-to-active
bindsym $mod+0 workspace 10, focus tiling; exec $map-to-active
#bindsym Shift+Ctrl+1 workspace 11, focus tiling
#bindsym Shift+Ctrl+2 workspace 12, focus tiling
#bindsym Shift+Ctrl+3 workspace 13, focus tiling
#bindsym Shift+Ctrl+4 workspace 14, focus tiling


# cycle between workspace
bindsym $mod+bracketright workspace next_on_output
bindsym $mod+bracketleft workspace prev_on_output

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+v move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10
#bindsym $mod+Ctrl+Shift+1 move container to workspace 11
#bindsym $mod+Ctrl+Shift+2 move container to workspace 12
#bindsym $mod+Ctrl+Shift+3 move container to workspace 13
#bindsym $mod+Ctrl+Shift+4 move container to workspace 14

# Move to a new empty workspace
bindsym $mod+Ctrl+h exec --no-startup-id ~/.bin/i3empty.py prev
bindsym $mod+Ctrl+l exec --no-startup-id ~/.bin/i3empty.py next

# Move a window to a new empty workspace
bindsym $mod+Shift+Ctrl+h exec --no-startup-id i3empty.py --move prev
bindsym $mod+Shift+Ctrl+l exec --no-startup-id i3empty.py --move next

## Move to empty workspace near a given workspace
#bindsym $mod+Mod1+1 exec --no-startup-id i3empty.py next 1
#bindsym $mod+Mod1+2 exec --no-startup-id i3empty.py next 2
#bindsym $mod+Mod1+3 exec --no-startup-id i3empty.py next 3
#bindsym $mod+Mod1+4 exec --no-startup-id i3empty.py next 4
#bindsym $mod+Mod1+5 exec --no-startup-id i3empty.py next 5
#bindsym $mod+Mod1+6 exec --no-startup-id i3empty.py next 6
#bindsym $mod+Mod1+7 exec --no-startup-id i3empty.py next 7
#bindsym $mod+Mod1+8 exec --no-startup-id i3empty.py next 8
#bindsym $mod+Mod1+9 exec --no-startup-id i3empty.py next 9
#bindsym $mod+Mod1+0 exec --no-startup-id i3empty.py next 10



# Move the current workspace to the next output
# (effectively toggles when you only have two outputs)
bindsym $mod+x move workspace to output right

# Put this window on the presentation output.
bindsym $mod+Shift+x move container to output right

# Assign workspace to the right monitor
workspace 1 output primary
workspace 2 output secondary

#================================================
#================Output Manag====================
#================================================


output $mon0 adaptive_sync on mode 2560x1440@164.999Hz pos 0 0
output $mon1 pos 2560 0 mode 3840x2160@60.000Hz

focus output $mon0

# Swap workspaces between displays
bindsym $mod+c exec ~/.bin/sway-display-swap.sh

# Assing certain workspaces to certain outputs
#workspace 10 output HDMI-0
workspace 9 output $mon0
#workspace 6 output HDMI-0
workspace 1 output $mon0

# read 1 character and mark the current window with this character
bindsym $mod+m exec i3-input -F 'mark %s' -l 1 -P 'Mark: '

# read 1 character and go to the window with the character
bindsym $mod+g exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '

#================================================
#====================Misc========================
#================================================

bindsym $mod+Shift+semicolon exec qutebrowser http://127.0.0.1:3875
bindsym $mod+semicolon exec makoctl dismiss --all

seat seat0 hide_cursor 3000

bindsym $mod+Shift+i exec kitty --class bookmarkViewer -e "oil"

# Eject USB menu
bindsym $mod+Shift+y exec ejectUSB

# Energy plan menu
bindsym $mod+z exec ~/.bin/energyPlanMenu

# Suspend computer
bindsym $mod+Pause exec systemctl suspend

bindsym $mod+Ctrl+o exec  "rofi -show run -font \\"DejaVu 9\\" -run-shell-command '{terminal} -e \\" {cmd}; read -n 1 -s\\"'"

# keepmenu
#bindsym $mod+a exec kpmenu -m wofi

# Manage audio
bindsym $mod+comma exec amixer set Master -q 5%-
bindsym $mod+period exec amixer set Master -q 5%+
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer set Master -q 5%+
bindsym XF86AudioLowerVolume exec --no-startup-id amixer set Master -q 5%-
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym $mod+dead_tilde exec playerctl play-pause
bindsym $mod+Shift+comma exec playerctl previous
bindsym $mod+Shift+period exec playerctl next

bindsym --locked XF86MonBrightnessUp exec brightnessctl s +5%
bindsym --locked XF86MonBrightnessDown exec brightnessctl s 5%-

# switch sound in active window between sinks
#bindsym $mod+p exec ~/.bin/sinkSwitcher
bindsym $mod+p exec ~/.bin/portSwitch

# switch default sink
#bindsym $mod+Shift+p exec ~/.bin/sinkDefaultSwitcher

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# rofi powermenu
# bindsym $mod+Shift+e exec ~/.Tools/bin/rofi-close-i3.sh
bindsym $mod+Shift+e exec ~/.config/polybar/scripts/pmenu_g

# printscreen
#bindsym --release Print       exec  scrot '%Y%m%d%H%M%S_screenshot_$wx$h.png' -e 'mv $f ~/Imagens/Screenshots && xclip -selection clipboard -t image/png -i ~/Imagens/Screenshots/$f'
#bindsym --release Shift+Print exec  scrot '%Y%m%d%H%M%S_screenshot_slice_$wx$h.png' -s -e 'mv $f ~/Imagens/Screenshots && xclip -selection clipboard -t image/png -i ~/Imagens/Screenshots/$f'
#bindsym --release Ctrl+Shift+Print exec  scrot '%Y%m%d%H%M%S_windowshot_$wx$h.png' -u -e 'mv $f ~/Imagens/Screenshots && xclip -selection clipboard -t image/png -i ~/Imagens/Screenshots/$f'

bindsym --release Print       exec grimshot copy output
bindsym --release Shift+Print exec grimshot copy window
bindsym --release Ctrl+Shift+Print exec grimshot copy area


# vpnufba
bindsym $mod+Shift+t exec nmcli connection up vpnufba

#================================================
#==================Cosmetics=====================
#================================================


# Disable titlebar
default_border pixel 3px

set $innerGap 20
set $outerGap 6

# Only enable gaps on a workspace when there is at least one container
#smart_gaps on

# Only enable outer gaps when there is exactly one container
#smart_gaps inverse_outer

# Hide edge borders only if there is one window with no gaps
#hide_edge_borders smart_no_gaps

# Activate smart borders (always)
smart_borders on

# Activate smart borders (only when there are effectively no gaps)
smart_borders no_gaps

# Gaps (i3-gaps)
gaps inner $innerGap
gaps outer $outerGap
#workspace 1 gaps inner 10
#workspace 1 gaps outer 20
#workspace 9 gaps inner 10
#workspace 9 gaps outer 2

# windows
bindsym $mod+Shift+f gaps inner current toggle $innerGap; gaps outer current toggle $outerGap

#for_window [class="qutebrowser"] gaps inner current set 0; gaps outer current set 0
#for_window [class="firefox"] gaps inner current set 10; gaps outer current set 0

# Window Border color
client.focused              #ffffff #ffffff #000000 #c8395b
client.urgent               #900000 #900000 #ffffff #c8395b

# Enable popup during fullscreen
popup_during_fullscreen smart

# Mouse, please stop focus whenever you hover to whatever windows plz :(
focus_follows_mouse no

#Lock screen binding.
#bindsym $mod+shift+z exec betterlockscreen -l dim
#bindsym $mod+shift+z exec swaylock --image `find $wallpapers_path -type f | shuf -n 1`
bindsym $mod+shift+z exec swaylock --clock --indicator --fade-in 0.2 --screenshots --effect-vignette 0.5:0.5 --effect-blur 7x5
#bindsym $mod+shift+z exec swaylock -c 000000
#bindsym $mod+shift+z exec output DP-2 dpsm on
         #timeout 30 'swaylock -f -c 000000' \

exec swayidle -w \
         timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
         before-sleep 'swaylock --clock --indicator --fade-in 0.2 --screenshots --effect-vignette 0.5:0.5 --effect-blur 7x5' \

for_window [class=".*"] inhibit_idle fullscreen
for_window [app_id=".*"] inhibit_idle fullscreen


# set desktop background with custom effect
#exec  betterlockscreen -w dim

# change wallpaper
set $oguriWallpaper $HOME/.bin/oguriWallpaper
set $animatedWallpaper $HOME/.bin/animatedWallpaper
bindsym --release $mod+i  exec $oguriWallpaper static
#bindsym --release $mod+dead_acute exec $animatedWallpaper
set $wallpapers_path $HOME/Files/Imagens/Favoritos
#output $mon0 bg `find $wallpapers_path -type f | shuf -n 1` fill
#output $mon1 bg `find $wallpapers_path -type f | shuf -n 1` fill
exec --no-startup-id $oguriWallpaper static
#exec --no-startup-id $animatedWallpaper static
#exec ~/.bin/changeWallpaper

#bindsym $mod+i exec output $mon0 bg `find $wallpapers_path -type f | shuf -n 1` fill; exec output $mon1 bg `find $wallpapers_path -type f | shuf -n 1` fill


#================================================
#====================Startup=====================
#================================================

## Nvidia settings

# Launch Bar
#exec_always  ~/.config/polybar/launch.sh &

exec_always --no-startup-id autotiling


#assign [class="firefox"] 9
#assign [instance="Navigator"] 9
assign [app_id="org.qutebrowser.qutebrowser"] 1
assign [class="Cheese"] 2
for_window [class="Cheese"] floating enable, resize set 1920 1080, move position center

for_window [app_id="org.qbittorrent.qBittorrent"] move scratchpad
for_window [app_id="planner"] move scratchpad
for_window [instance="teams-for-linux"] move scratchpad, resize set 1600 900, move position 160px 100px"
for_window [instance="music.youtube.com"] move scratchpad, resize set 1600 900, move position 160px 100px"
for_window [instance="spotify"] move scratchpad, resize set 1600 900, move position 160px 100px"
for_window [instance="trello.com__b_PtPUWPeM_doutorado-e-vida-profissional"] move scratchpad, resize set 1600 900, move position 160px 100px"
for_window [app_id="com.github.wwmm.easyeffects"] move scratchpad
for_window [class="Element"] move scratchpad
for_window [class="steam"] floating enable
for_window [title="Firefox — Sharing Indicator"] move scratchpad
for_window [class="Slack"] move scratchpad
for_window [instance="origin.exe"] floating enable
for_window [class="origin.exe"] floating enable
for_window [app_id="kotatogramdesktop"] move scratchpad, resize set 1600 900, move position 160px 100px
for_window [app_id="com.rafaelmardojai.Blanket"] move scratchpad
#for_window [class="Transmission-gtk"] move scratchpad
#for_window [app_id="org.keepassxc.KeePassXC"] move scratchpad
for_window [class="Bitwarden"] move scratchpad
for_window [class="youtube-music-desktop-app"] move scratchpad, resize set 1600 900, move position 160px 100px"
#for_window [class="ICE-SSB-youtubemusic"] move scratchpad, resize set 1600 900, move position 160px 100px"
for_window [class="Display"] floating enable
for_window [instance="bookmarkViewer"] floating enable, resize set 1800 300, move position center, move down 300 px
for_window [instance="dropdown_terminal"] floating enable, sticky enable, resize set 1800 400, move position center, move down 300 px
#for_window [title="mpv "] move scratchpad
#for_window [title="Steam" floating] move scratchpad

exec --no-startup-id systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec --no-startup-id hash dbus-update-activation-environment 2>/dev/null && \
     dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec --no-startup-id mako
exec --no-startup-id fcitx5 -d
#exec sleep 2 ; exec nice -n 0 qutebrowser
#exec nice -n 0 waybar
#exec_always nice -n 20  steam -silent
#exec  ulauncher
exec --no-startup-id sleep 10; exec --no-startup-id qbittorrent
exec --no-startup-id kotatogram-desktop
exec --no-startup-id udiskie
#exec --no-startup-id flatpak run com.valvesoftware.Steam -silent -pipewire-dmabuf
#exec  sleep 5 ; exec ~/.bin/remote
#exec  sleep 30 ; exec  safeeyes
#exec  sleep 40 ; exec ytmdesktop
exec --no-startup-id ~/.bin/remote
#exec easyeffects
#exec  telegram-desktop
exec  --no-startup-id blueman-tray
exec  --no-startup-id transmission-qt
exec  --no-startup-id wlsunset -l -12.97111 -L -38.51083
exec  --no-startup-id $todo
exec  --no-startup-id sleep 20; exec  --no-startup-id dropbox
exec  --no-startup-id sleep 20; exec  --no-startup-id flatpak run nz.mega.MEGAsync
exec  --no-startup-id sleep 10; exec  --no-startup-id jellyfin-mpv-shim
#exec  hookBackup

#================================================
#====================Modes=======================
#================================================
mode "work" {
        # open youtube music
        bindsym 1 exec ~/mestrado/Códigos/LSRTM_GPU_proj/projects/FreqRTM/work;mode "default"

        bindsym 2 exec sandboxi3wm;mode "default"

        bindsym 3 exec exec $term -e taskwarrior-tui;mode "default"

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+y mode "work"

mode "apps" {
        # open youtube music
        #bindsym m exec ytmdesktop;mode "default"
        bindsym m exec $music;[instance="spotify"] scratchpad show; mode "default"
        #bindsym m exec firefox --class ICE-SSB-youtubemusic --profile $HOME/.local/share/ice/$firefox/youtubemusic --no-remote https://music.youtube.com;mode "default"

        # open netflix
        bindsym n exec firefox "netflix.com";mode "default"

        # open calendar
        bindsym q exec $browser "calendar.google.com";mode "default"

        # open keepass
        #bindsym c exec keepassxc;mode "default"
        bindsym c exec bitwarden;mode "default"

        # open planner
        bindsym $mod+p exec planner;mode "default"

        # open whatsapp
        bindsym z exec firefox "https://web.whatsapp.com/";mode "default"

        # INVESTIR POHA
        bindsym i exec $firefox $site1 $site2 $site3 $site4 $site5 $site6;mode "default"

        # Kanji
        bindsym k exec zathura "~/Documentos/Remembering Kanji/Remembering the Kanji 1 (Kindle Fire HDX edition)_d Writing of Japanese Characters - James W. Heisig.pdf" &;mode "default"

        # steam
        #bindsym s exec steam-native -nojoy -novid -d3d9ex -high;mode "default"
        #bindsym s exec steam;mode "default"
        bindsym s exec flatpak run com.valvesoftware.Steam;mode "default"

        # lutris
        bindsym d exec env -u WAYLAND_DISPLAY lutris;mode "default"

        # yuzu
        bindsym y exec "QT_QPA_PLATFORM=xcb yuzu";mode "default"

        # Elements
        bindsym e exec element-desktop;mode "default"

        # blanket
        bindsym b exec blanket;mode "default"

        # $firefox
        bindsym f exec $firefox;mode "default"

        # teams
        bindsym t exec $teams;mode "default"

        # trello
        bindsym w exec $todo;mode "default"

        # $firefox private
        bindsym g exec $firefox -private-window;mode "default"

        # inkscape
        bindsym j exec inkscape;mode "default"

        # pavucontrol
        bindsym p exec pavucontrol;mode "default"

        # cmus
        bindsym v exec kitty -e cmus;mode "default"

        # htop
        bindsym h exec kitty -e htop;mode "default"

        # piper
        bindsym l exec piper;mode "default"

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+q mode "apps"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.

        bindsym h resize shrink width 6 px or 6 ppt
        bindsym j resize grow height 6 px or 6 ppt
        bindsym k resize shrink height 6 px or 6 ppt
        bindsym l resize grow width 6 px or 6 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 6 px or 6 ppt
        bindsym Down resize grow height 6 px or 6 ppt
        bindsym Up resize shrink height 6 px or 6 ppt
        bindsym Right resize grow width 6 px or 6 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

input "type:keyboard" {
    xkb_layout br,us
    xkb_variant ,dvp
    xkb_options caps:swapescape,grp:rwin_toggle
}

bar {
    swaybar_command waybar
    position top

    # Keep in mind that the current directory of this config file is $HOME
    #status_command while ~/.config/sway/status.sh; do sleep 1; done

    colors {
        # Text color of status bar
        statusline #ffffff
        # Background of status bar
        #background #323232
        background #323232

        focused_workspace #E8DF3F #DE8E10 #071713
    }

    # When the status_command prints a new line to stdout, swaybar updates.
    # The default just shows the current date and time.
    status_command while date +'%-d/%-m/%Y  %H:%M:%S'; do sleep 1; done
    #status_command  i3status-rs
    height 30
    font pango:RobotoMono 11, FontAwesome 12
}

bindsym $mod+Shift+d exec kitty -t 'launcher' -e /usr/bin/sway-launcher-desktop
for_window [app_id="kitty" title="^launcher$"] floating enable; border pixel 4; sticky enable



##Borders
#border_images.focused $HOME/.config/sway/lightbord.png
#border_images.focused_inactive $HOME/.config/sway/lightbord.png
#border_images.unfocused $HOME/.config/sway/darkbord.png
#border_images.urgent $HOME/.config/sway/lightbord.png

## Defining colors         border     bg         text       indicator  child_border
#client.focused        	 	#12151d    #12151d    #abb2bf    #c678dd    #12151d
#client.unfocused        	#1e222a    #1e222a    #abb2bf	 #282c34    #282c34
#client.focused_inactive 	#1e222a    #1e222a    #abb2bf    #282c34    #282c34
#client.urgent	          	#e06c75    #1e222a    #abb2bf    #e06c75    #e06c75

input 1133:49271:Logitech_USB_Optical_Mouse {
    natural_scroll disabled
    accel_profile "adaptive" # disable mouse acceleration (enabled by default; to set it manually, use "adaptive" instead of "flat")
    pointer_accel 0.5 # set mouse sensitivity (between -1 and 1)
}
input 16700:8467:Dell_KB216_Wired_Keyboard {
    xkb_layout us
    xkb_options caps:swapescape
}
input 16700:8455:Dell_Dell_USB_Entry_Keyboard {
    xkb_layout us
    xkb_options caps:swapescape
}
input "1:1:AT_Translated_Set_2_keyboard" {
    xkb_layout us,us
    xkb_variant altgr-intl,dvp
    xkb_options caps:swapescape
}
input 1386:890:Wacom_One_by_Wacom_S_Pen {
    #left_handed "enabled"
}
input type:touchpad {
    tap "enabled"
}
